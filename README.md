# My Dotfiles
## Installation
```bash
git clone https://github.com/kevin-pietruszka/dotfiles.git && \
cd dotfiles && \
source install.sh && \
chsh -s $(which zsh)
```
Restart the shell

### TMUX
`prefix` + <kbd>I</kbd>
- Installs plugins and reloads tmux
